---
import ArticleIndex from "$layouts/ArticleIndex.astro";

export async function getStaticPaths() {
  return ["zh", "en"].map((lang) => ({
    params: { lang },
  }));
}
---

<ArticleIndex lang={Astro.params.lang} />
